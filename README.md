# Hackernews clone - Frontend
Tech used: React, Apollo, GraphQL

### In the project directory, you can run:
*Run Frontend Server*
- `npm install`
- `npm start`
- `npm test`
- `npm run build`

*Run Backend Server*
- `cd server`
- `npm install`
- `npm start`
